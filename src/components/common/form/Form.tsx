import * as Styles from "./styles";

const Form = () => {
  return (
    <Styles.Form>
      <Styles.Input placeholder="Email adress" />
      <Styles.Input placeholder="Password" />
    </Styles.Form>
  );
};

export { Form };
